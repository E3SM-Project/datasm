WAREHOUSE:Ready:
    atm-ts-mon:
        - POSTPROCESS:Ready
    atm-climo-mon:
        - POSTPROCESS:Ready
    atm-cmip-mon:
        - POSTPROCESS:Ready
    atm-cmip-day:
        - POSTPROCESS:Ready
    atm-cmip-3hr:
        - POSTPROCESS:Ready
    lnd-ts-mon:
        - POSTPROCESS:Ready
    lnd-cmip-mon:
        - POSTPROCESS:Ready
    ocn-cmip-mon:
        - POSTPROCESS:Ready
    sea-ice-cmip-mon:
        - POSTPROCESS:Ready
    default:
        - EXTRACTION:Ready

EXTRACTION:Pass:
    default:
        - Validation:Ready
EXTRACTION:Fail:
    default:
        - Exit:Fail

VALIDATION:Pass:
    default:
        - PUBLICATION:Ready
VALIDATION:Fail:
    default:
        - Exit:Fail

POSTPROCESS:Pass:
    default:
        - PUBLICATION:Ready
POSTPROCESS:Fail:
    default:
        - Exit:Fail

PUBLICATION:Pass:
    default:
        - CLEANUP:Ready
PUBLICATION:Fail:
    default:
        - Exit:Fail

CLEANUP:Pass:
    default:
        - WAREHOUSE:Pass
CLEANUP:Fail:
    default:
        - Exit:Fail
