# Conda production environment for datasm production operations (`extract`, `validate`, and `postprocess`)
name: datasm_prod
channels:
  - conda-forge
  - e3sm
  - defaults
dependencies:
  # Base
  # ==================
  - python=3.9.10
  - pip=22.0.3
  - datasm=0.1.0 # NOTE: This package has not been released on Conda yet.
prefix: /opt/miniconda3/envs/datasm_prod
